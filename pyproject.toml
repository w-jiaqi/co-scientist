[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=64.0.0", "setuptools-scm>=8"]

[project]
name = 'genbio_leaderboard'
description = 'Temporary description'
readme = 'README.md'
requires-python = '>= 3.11'
authors = [{ name = 'Caleb Ellington' }]
maintainers = [{ name = 'Caleb Ellington' }]
classifiers = [
    'Development Status :: 4 - Beta',
    'Intended Audience :: Science/Research',
    'Natural Language :: English',
    'Operating System :: OS Independent',
    'Programming Language :: Python',
    'Programming Language :: Python :: 3 :: Only',
    'Programming Language :: Python :: 3.11',
    'Programming Language :: Python :: 3.12',
    'Typing :: Typed',
]

dynamic = ['version']

dependencies = [
    'anndata',
    'pandas',
    'torch',
    'numpy',
    'cloudpathlib[gs]',
    'datasets',
    'torchmetrics',
    'scanpy',
    'flask',
    'ipykernel',
    'appdirs>=1.4.4',
    "py7zr>0.20.0",
    "polaris-lib>=0.8.6",
    "zarr<3",
    'typer[all]>=0.9.0',
    'rich>=13.0',
    'jinja2>=3.1',
    'optuna>=3.0',
    'scikit-learn>=1.3',
    'joblib>=1.3',
    'matplotlib>=3.7',
    'seaborn>=0.12',
    'pyyaml>=6.0',
    'scipy',
]

[project.optional-dependencies]
pipelines = [
    "kfp[kubernetes]",
]
llm = [
    "anthropic>=0.20",
    "openai>=1.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"genbio.datasets" = [
    "**/*.py",
    "**/*.csv",
    "**/*.yaml",
    "**/*.yml",
    "**/*.md"
]
"genbio.coscientist" = [
    "templates/**/*",
]

[project.scripts]
genbio-leaderboard = "genbio.leaderboard.reporting:cli"
cosci = "genbio.coscientist.cli:app"
